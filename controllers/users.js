module.exports.setUserByChat = (chat) => {
// сохранили пользователя по id из чата message.chat
   const chat = {
        id: 134167611,
            first_name: 'Vladislav',
            username: 'borovkov_vlad',
            type: 'private'
    }
}

module.exports.getUserByChat = (chat) => {
// получили пользователя по id из чата
}

module.exports.setUserLocale = (id, locale) => {
// получили пользователя по id из чата
}

module.exports.getUserLocale = (id) => {
// получили пользователя по id из чата

// возвращает строку с локалью из конфига
}
